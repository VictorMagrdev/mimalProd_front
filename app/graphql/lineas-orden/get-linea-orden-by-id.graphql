query GetLineaOrdenById($id: ID!) {
  lineaOrden(id: $id) {
    id
    numeroLinea
    cantidadRequerida
    cantidadUsada
    costoUnitario
    observaciones
    idOrden: orden { id }
    idProductoComponente: productoComponente { id }
    idUnidadComponente: unidadComponente { id }
  }
}
